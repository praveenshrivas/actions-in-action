name: Context Demo

on:
  push:
  workflow_dispatch:

jobs:
  context-demo:

    runs-on: ubuntu-latest

    steps:

      - name: Display Runner 
        if: github.ref == 'refs/heads/main'
        run: echo "Deploying to Production..."
